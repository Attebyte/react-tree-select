# Cloudflare Pages (Git-connected).
# Dashboard build command must be `npm run build-site` (not `npm run build`,
# which emits the npm library to dist/).
name = "react-tree-select"
compatibility_date = "2026-08-29"
pages_build_output_dir = "./hosting"
